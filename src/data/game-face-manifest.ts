/**
 * Exact-year face assets are opt-in and card-specific. Add an id only after
 * placing its PNG in the matching local directory and recording complete
 * reusable-source metadata in player-images.ts.
 */
export const playerGameFaceCardIds = [] as const;
export const managerGameFaceCardIds = [] as const;
